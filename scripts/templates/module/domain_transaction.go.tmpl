package domain

import "context"

// {{PASCAL}}Repositories groups {{MODULE}}-context persistence ports for one transaction.
type {{PASCAL}}Repositories struct {
	// TODO: add repository fields.
	{{PASCAL}} {{PASCAL}}Repository
}

// Run{{PASCAL}}InTransactionFunc runs fn with repos bound to a single DB transaction.
type Run{{PASCAL}}InTransactionFunc func(
	ctx context.Context,
	fn func(ctx context.Context, repos {{PASCAL}}Repositories) error,
) error
