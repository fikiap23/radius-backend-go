// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/radius/radius-backend/ent/task"
	"github.com/radius/radius-backend/ent/taskchecklistitem"
)

// TaskChecklistItem is the model entity for the TaskChecklistItem schema.
type TaskChecklistItem struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// TaskID holds the value of the "task_id" field.
	TaskID string `json:"task_id,omitempty"`
	// Text holds the value of the "text" field.
	Text string `json:"text,omitempty"`
	// Checked holds the value of the "checked" field.
	Checked bool `json:"checked,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TaskChecklistItemQuery when eager-loading is set.
	Edges        TaskChecklistItemEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TaskChecklistItemEdges holds the relations/edges for other nodes in the graph.
type TaskChecklistItemEdges struct {
	// Task holds the value of the task edge.
	Task *Task `json:"task,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TaskOrErr returns the Task value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TaskChecklistItemEdges) TaskOrErr() (*Task, error) {
	if e.Task != nil {
		return e.Task, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: task.Label}
	}
	return nil, &NotLoadedError{edge: "task"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TaskChecklistItem) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case taskchecklistitem.FieldChecked:
			values[i] = new(sql.NullBool)
		case taskchecklistitem.FieldID, taskchecklistitem.FieldTaskID, taskchecklistitem.FieldText:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TaskChecklistItem fields.
func (tci *TaskChecklistItem) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case taskchecklistitem.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				tci.ID = value.String
			}
		case taskchecklistitem.FieldTaskID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field task_id", values[i])
			} else if value.Valid {
				tci.TaskID = value.String
			}
		case taskchecklistitem.FieldText:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field text", values[i])
			} else if value.Valid {
				tci.Text = value.String
			}
		case taskchecklistitem.FieldChecked:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field checked", values[i])
			} else if value.Valid {
				tci.Checked = value.Bool
			}
		default:
			tci.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TaskChecklistItem.
// This includes values selected through modifiers, order, etc.
func (tci *TaskChecklistItem) Value(name string) (ent.Value, error) {
	return tci.selectValues.Get(name)
}

// QueryTask queries the "task" edge of the TaskChecklistItem entity.
func (tci *TaskChecklistItem) QueryTask() *TaskQuery {
	return NewTaskChecklistItemClient(tci.config).QueryTask(tci)
}

// Update returns a builder for updating this TaskChecklistItem.
// Note that you need to call TaskChecklistItem.Unwrap() before calling this method if this TaskChecklistItem
// was returned from a transaction, and the transaction was committed or rolled back.
func (tci *TaskChecklistItem) Update() *TaskChecklistItemUpdateOne {
	return NewTaskChecklistItemClient(tci.config).UpdateOne(tci)
}

// Unwrap unwraps the TaskChecklistItem entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tci *TaskChecklistItem) Unwrap() *TaskChecklistItem {
	_tx, ok := tci.config.driver.(*txDriver)
	if !ok {
		panic("ent: TaskChecklistItem is not a transactional entity")
	}
	tci.config.driver = _tx.drv
	return tci
}

// String implements the fmt.Stringer.
func (tci *TaskChecklistItem) String() string {
	var builder strings.Builder
	builder.WriteString("TaskChecklistItem(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tci.ID))
	builder.WriteString("task_id=")
	builder.WriteString(tci.TaskID)
	builder.WriteString(", ")
	builder.WriteString("text=")
	builder.WriteString(tci.Text)
	builder.WriteString(", ")
	builder.WriteString("checked=")
	builder.WriteString(fmt.Sprintf("%v", tci.Checked))
	builder.WriteByte(')')
	return builder.String()
}

// TaskChecklistItems is a parsable slice of TaskChecklistItem.
type TaskChecklistItems []*TaskChecklistItem
