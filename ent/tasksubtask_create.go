// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/radius/radius-backend/ent/task"
	"github.com/radius/radius-backend/ent/tasksubtask"
)

// TaskSubtaskCreate is the builder for creating a TaskSubtask entity.
type TaskSubtaskCreate struct {
	config
	mutation *TaskSubtaskMutation
	hooks    []Hook
}

// SetTaskID sets the "task_id" field.
func (tsc *TaskSubtaskCreate) SetTaskID(s string) *TaskSubtaskCreate {
	tsc.mutation.SetTaskID(s)
	return tsc
}

// SetTitle sets the "title" field.
func (tsc *TaskSubtaskCreate) SetTitle(s string) *TaskSubtaskCreate {
	tsc.mutation.SetTitle(s)
	return tsc
}

// SetDone sets the "done" field.
func (tsc *TaskSubtaskCreate) SetDone(b bool) *TaskSubtaskCreate {
	tsc.mutation.SetDone(b)
	return tsc
}

// SetNillableDone sets the "done" field if the given value is not nil.
func (tsc *TaskSubtaskCreate) SetNillableDone(b *bool) *TaskSubtaskCreate {
	if b != nil {
		tsc.SetDone(*b)
	}
	return tsc
}

// SetID sets the "id" field.
func (tsc *TaskSubtaskCreate) SetID(s string) *TaskSubtaskCreate {
	tsc.mutation.SetID(s)
	return tsc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (tsc *TaskSubtaskCreate) SetNillableID(s *string) *TaskSubtaskCreate {
	if s != nil {
		tsc.SetID(*s)
	}
	return tsc
}

// SetTask sets the "task" edge to the Task entity.
func (tsc *TaskSubtaskCreate) SetTask(t *Task) *TaskSubtaskCreate {
	return tsc.SetTaskID(t.ID)
}

// Mutation returns the TaskSubtaskMutation object of the builder.
func (tsc *TaskSubtaskCreate) Mutation() *TaskSubtaskMutation {
	return tsc.mutation
}

// Save creates the TaskSubtask in the database.
func (tsc *TaskSubtaskCreate) Save(ctx context.Context) (*TaskSubtask, error) {
	tsc.defaults()
	return withHooks(ctx, tsc.sqlSave, tsc.mutation, tsc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (tsc *TaskSubtaskCreate) SaveX(ctx context.Context) *TaskSubtask {
	v, err := tsc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tsc *TaskSubtaskCreate) Exec(ctx context.Context) error {
	_, err := tsc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tsc *TaskSubtaskCreate) ExecX(ctx context.Context) {
	if err := tsc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (tsc *TaskSubtaskCreate) defaults() {
	if _, ok := tsc.mutation.Done(); !ok {
		v := tasksubtask.DefaultDone
		tsc.mutation.SetDone(v)
	}
	if _, ok := tsc.mutation.ID(); !ok {
		v := tasksubtask.DefaultID
		tsc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (tsc *TaskSubtaskCreate) check() error {
	if _, ok := tsc.mutation.TaskID(); !ok {
		return &ValidationError{Name: "task_id", err: errors.New(`ent: missing required field "TaskSubtask.task_id"`)}
	}
	if _, ok := tsc.mutation.Title(); !ok {
		return &ValidationError{Name: "title", err: errors.New(`ent: missing required field "TaskSubtask.title"`)}
	}
	if v, ok := tsc.mutation.Title(); ok {
		if err := tasksubtask.TitleValidator(v); err != nil {
			return &ValidationError{Name: "title", err: fmt.Errorf(`ent: validator failed for field "TaskSubtask.title": %w`, err)}
		}
	}
	if _, ok := tsc.mutation.Done(); !ok {
		return &ValidationError{Name: "done", err: errors.New(`ent: missing required field "TaskSubtask.done"`)}
	}
	if len(tsc.mutation.TaskIDs()) == 0 {
		return &ValidationError{Name: "task", err: errors.New(`ent: missing required edge "TaskSubtask.task"`)}
	}
	return nil
}

func (tsc *TaskSubtaskCreate) sqlSave(ctx context.Context) (*TaskSubtask, error) {
	if err := tsc.check(); err != nil {
		return nil, err
	}
	_node, _spec := tsc.createSpec()
	if err := sqlgraph.CreateNode(ctx, tsc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(string); ok {
			_node.ID = id
		} else {
			return nil, fmt.Errorf("unexpected TaskSubtask.ID type: %T", _spec.ID.Value)
		}
	}
	tsc.mutation.id = &_node.ID
	tsc.mutation.done = true
	return _node, nil
}

func (tsc *TaskSubtaskCreate) createSpec() (*TaskSubtask, *sqlgraph.CreateSpec) {
	var (
		_node = &TaskSubtask{config: tsc.config}
		_spec = sqlgraph.NewCreateSpec(tasksubtask.Table, sqlgraph.NewFieldSpec(tasksubtask.FieldID, field.TypeString))
	)
	if id, ok := tsc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := tsc.mutation.Title(); ok {
		_spec.SetField(tasksubtask.FieldTitle, field.TypeString, value)
		_node.Title = value
	}
	if value, ok := tsc.mutation.Done(); ok {
		_spec.SetField(tasksubtask.FieldDone, field.TypeBool, value)
		_node.Done = value
	}
	if nodes := tsc.mutation.TaskIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   tasksubtask.TaskTable,
			Columns: []string{tasksubtask.TaskColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(task.FieldID, field.TypeString),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.TaskID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// TaskSubtaskCreateBulk is the builder for creating many TaskSubtask entities in bulk.
type TaskSubtaskCreateBulk struct {
	config
	err      error
	builders []*TaskSubtaskCreate
}

// Save creates the TaskSubtask entities in the database.
func (tscb *TaskSubtaskCreateBulk) Save(ctx context.Context) ([]*TaskSubtask, error) {
	if tscb.err != nil {
		return nil, tscb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(tscb.builders))
	nodes := make([]*TaskSubtask, len(tscb.builders))
	mutators := make([]Mutator, len(tscb.builders))
	for i := range tscb.builders {
		func(i int, root context.Context) {
			builder := tscb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*TaskSubtaskMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, tscb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, tscb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, tscb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (tscb *TaskSubtaskCreateBulk) SaveX(ctx context.Context) []*TaskSubtask {
	v, err := tscb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tscb *TaskSubtaskCreateBulk) Exec(ctx context.Context) error {
	_, err := tscb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tscb *TaskSubtaskCreateBulk) ExecX(ctx context.Context) {
	if err := tscb.Exec(ctx); err != nil {
		panic(err)
	}
}
