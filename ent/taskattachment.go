// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/radius/radius-backend/ent/task"
	"github.com/radius/radius-backend/ent/taskattachment"
)

// TaskAttachment is the model entity for the TaskAttachment schema.
type TaskAttachment struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// TaskID holds the value of the "task_id" field.
	TaskID string `json:"task_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Size holds the value of the "size" field.
	Size int64 `json:"size,omitempty"`
	// MimeType holds the value of the "mime_type" field.
	MimeType string `json:"mime_type,omitempty"`
	// StorageKey holds the value of the "storage_key" field.
	StorageKey string `json:"storage_key,omitempty"`
	// URL holds the value of the "url" field.
	URL string `json:"url,omitempty"`
	// UploadedAt holds the value of the "uploaded_at" field.
	UploadedAt time.Time `json:"uploaded_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TaskAttachmentQuery when eager-loading is set.
	Edges        TaskAttachmentEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TaskAttachmentEdges holds the relations/edges for other nodes in the graph.
type TaskAttachmentEdges struct {
	// Task holds the value of the task edge.
	Task *Task `json:"task,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TaskOrErr returns the Task value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TaskAttachmentEdges) TaskOrErr() (*Task, error) {
	if e.Task != nil {
		return e.Task, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: task.Label}
	}
	return nil, &NotLoadedError{edge: "task"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TaskAttachment) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case taskattachment.FieldSize:
			values[i] = new(sql.NullInt64)
		case taskattachment.FieldID, taskattachment.FieldTaskID, taskattachment.FieldName, taskattachment.FieldMimeType, taskattachment.FieldStorageKey, taskattachment.FieldURL:
			values[i] = new(sql.NullString)
		case taskattachment.FieldUploadedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TaskAttachment fields.
func (ta *TaskAttachment) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case taskattachment.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				ta.ID = value.String
			}
		case taskattachment.FieldTaskID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field task_id", values[i])
			} else if value.Valid {
				ta.TaskID = value.String
			}
		case taskattachment.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				ta.Name = value.String
			}
		case taskattachment.FieldSize:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field size", values[i])
			} else if value.Valid {
				ta.Size = value.Int64
			}
		case taskattachment.FieldMimeType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field mime_type", values[i])
			} else if value.Valid {
				ta.MimeType = value.String
			}
		case taskattachment.FieldStorageKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field storage_key", values[i])
			} else if value.Valid {
				ta.StorageKey = value.String
			}
		case taskattachment.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				ta.URL = value.String
			}
		case taskattachment.FieldUploadedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field uploaded_at", values[i])
			} else if value.Valid {
				ta.UploadedAt = value.Time
			}
		default:
			ta.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TaskAttachment.
// This includes values selected through modifiers, order, etc.
func (ta *TaskAttachment) Value(name string) (ent.Value, error) {
	return ta.selectValues.Get(name)
}

// QueryTask queries the "task" edge of the TaskAttachment entity.
func (ta *TaskAttachment) QueryTask() *TaskQuery {
	return NewTaskAttachmentClient(ta.config).QueryTask(ta)
}

// Update returns a builder for updating this TaskAttachment.
// Note that you need to call TaskAttachment.Unwrap() before calling this method if this TaskAttachment
// was returned from a transaction, and the transaction was committed or rolled back.
func (ta *TaskAttachment) Update() *TaskAttachmentUpdateOne {
	return NewTaskAttachmentClient(ta.config).UpdateOne(ta)
}

// Unwrap unwraps the TaskAttachment entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ta *TaskAttachment) Unwrap() *TaskAttachment {
	_tx, ok := ta.config.driver.(*txDriver)
	if !ok {
		panic("ent: TaskAttachment is not a transactional entity")
	}
	ta.config.driver = _tx.drv
	return ta
}

// String implements the fmt.Stringer.
func (ta *TaskAttachment) String() string {
	var builder strings.Builder
	builder.WriteString("TaskAttachment(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ta.ID))
	builder.WriteString("task_id=")
	builder.WriteString(ta.TaskID)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(ta.Name)
	builder.WriteString(", ")
	builder.WriteString("size=")
	builder.WriteString(fmt.Sprintf("%v", ta.Size))
	builder.WriteString(", ")
	builder.WriteString("mime_type=")
	builder.WriteString(ta.MimeType)
	builder.WriteString(", ")
	builder.WriteString("storage_key=")
	builder.WriteString(ta.StorageKey)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(ta.URL)
	builder.WriteString(", ")
	builder.WriteString("uploaded_at=")
	builder.WriteString(ta.UploadedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TaskAttachments is a parsable slice of TaskAttachment.
type TaskAttachments []*TaskAttachment
