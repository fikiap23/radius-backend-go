// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/radius/radius-backend/ent/boardcolumn"
	"github.com/radius/radius-backend/ent/project"
)

// BoardColumn is the model entity for the BoardColumn schema.
type BoardColumn struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// ProjectID holds the value of the "project_id" field.
	ProjectID string `json:"project_id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// WipLimit holds the value of the "wip_limit" field.
	WipLimit *int `json:"wip_limit,omitempty"`
	// SortOrder holds the value of the "sort_order" field.
	SortOrder int `json:"sort_order,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BoardColumnQuery when eager-loading is set.
	Edges        BoardColumnEdges `json:"edges"`
	selectValues sql.SelectValues
}

// BoardColumnEdges holds the relations/edges for other nodes in the graph.
type BoardColumnEdges struct {
	// Project holds the value of the project edge.
	Project *Project `json:"project,omitempty"`
	// Tasks holds the value of the tasks edge.
	Tasks []*Task `json:"tasks,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ProjectOrErr returns the Project value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BoardColumnEdges) ProjectOrErr() (*Project, error) {
	if e.Project != nil {
		return e.Project, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: project.Label}
	}
	return nil, &NotLoadedError{edge: "project"}
}

// TasksOrErr returns the Tasks value or an error if the edge
// was not loaded in eager-loading.
func (e BoardColumnEdges) TasksOrErr() ([]*Task, error) {
	if e.loadedTypes[1] {
		return e.Tasks, nil
	}
	return nil, &NotLoadedError{edge: "tasks"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BoardColumn) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case boardcolumn.FieldWipLimit, boardcolumn.FieldSortOrder:
			values[i] = new(sql.NullInt64)
		case boardcolumn.FieldID, boardcolumn.FieldProjectID, boardcolumn.FieldTitle, boardcolumn.FieldStatus:
			values[i] = new(sql.NullString)
		case boardcolumn.FieldCreatedAt, boardcolumn.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BoardColumn fields.
func (bc *BoardColumn) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case boardcolumn.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				bc.ID = value.String
			}
		case boardcolumn.FieldProjectID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field project_id", values[i])
			} else if value.Valid {
				bc.ProjectID = value.String
			}
		case boardcolumn.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				bc.Title = value.String
			}
		case boardcolumn.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				bc.Status = value.String
			}
		case boardcolumn.FieldWipLimit:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field wip_limit", values[i])
			} else if value.Valid {
				bc.WipLimit = new(int)
				*bc.WipLimit = int(value.Int64)
			}
		case boardcolumn.FieldSortOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sort_order", values[i])
			} else if value.Valid {
				bc.SortOrder = int(value.Int64)
			}
		case boardcolumn.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				bc.CreatedAt = value.Time
			}
		case boardcolumn.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				bc.UpdatedAt = value.Time
			}
		default:
			bc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BoardColumn.
// This includes values selected through modifiers, order, etc.
func (bc *BoardColumn) Value(name string) (ent.Value, error) {
	return bc.selectValues.Get(name)
}

// QueryProject queries the "project" edge of the BoardColumn entity.
func (bc *BoardColumn) QueryProject() *ProjectQuery {
	return NewBoardColumnClient(bc.config).QueryProject(bc)
}

// QueryTasks queries the "tasks" edge of the BoardColumn entity.
func (bc *BoardColumn) QueryTasks() *TaskQuery {
	return NewBoardColumnClient(bc.config).QueryTasks(bc)
}

// Update returns a builder for updating this BoardColumn.
// Note that you need to call BoardColumn.Unwrap() before calling this method if this BoardColumn
// was returned from a transaction, and the transaction was committed or rolled back.
func (bc *BoardColumn) Update() *BoardColumnUpdateOne {
	return NewBoardColumnClient(bc.config).UpdateOne(bc)
}

// Unwrap unwraps the BoardColumn entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (bc *BoardColumn) Unwrap() *BoardColumn {
	_tx, ok := bc.config.driver.(*txDriver)
	if !ok {
		panic("ent: BoardColumn is not a transactional entity")
	}
	bc.config.driver = _tx.drv
	return bc
}

// String implements the fmt.Stringer.
func (bc *BoardColumn) String() string {
	var builder strings.Builder
	builder.WriteString("BoardColumn(")
	builder.WriteString(fmt.Sprintf("id=%v, ", bc.ID))
	builder.WriteString("project_id=")
	builder.WriteString(bc.ProjectID)
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(bc.Title)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(bc.Status)
	builder.WriteString(", ")
	if v := bc.WipLimit; v != nil {
		builder.WriteString("wip_limit=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("sort_order=")
	builder.WriteString(fmt.Sprintf("%v", bc.SortOrder))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(bc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(bc.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// BoardColumns is a parsable slice of BoardColumn.
type BoardColumns []*BoardColumn
